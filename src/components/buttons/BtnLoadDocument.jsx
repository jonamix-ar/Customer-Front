import React from 'react'

const BtnLoadDocument = ({ onClick }) => {
  return (
    <button onClick={onClick} className="btn btn-primary">
      Load Document
    </button>
  )
}

export default BtnLoadDocument
